#!/bin/bash

# Open the remote url of git project

function open_gitlab() {
    open $(git config remote.origin.url);
}

if [ -z "$1" ]; then
    open_gitlab
else
    (cd "$(project_root $1)" && open_gitlab)
fi
