#!/bin/sh

usage() {
    cat <<'EOF'
Usage: wip

Stages every change in the repository and commits them under a 'WIP'
message.
EOF
}

case "$1" in
    -h | --help)
        usage
        exit 0
        ;;
esac

git add . && git commit -m 'WIP'
