#!/bin/sh

usage() {
    cat <<'EOF'
Usage: cof <file>

Prints the sha256 checksum of <file>.
EOF
}

case "$1" in
    -h | --help)
        usage
        exit 0
        ;;
esac

if [ $# -lt 1 ]; then
    echo "Expected a file, see 'cof --help'" >&2
    exit 1
fi

shasum -a 256 "$1" | cut -d' ' -f1
