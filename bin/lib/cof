#!/bin/sh

usage() {
    cat <<'EOF'
Usage: cof <file>

Prints the sha256 checksum of <file>.
EOF
}

if [ $# -lt 1 ] || [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    usage
    exit 0
fi

shasum -a 256 "$1" | cut -d' ' -f1
