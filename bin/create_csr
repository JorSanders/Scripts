#!/bin/bash

if [ "$1" == "--help" ]; then
    echo "Create a csr"
    echo "\$1 key file .pem"
    echo "\$2 CN for the csr"
    exit 0
fi

openssl req -new -sha256 -key $1 -subj "/C=NL/ST=Zuid-Holland/L=Den Haag/O=Capgemini/OU=Doorontwikkeling/CN=$2" -out cert.csr
